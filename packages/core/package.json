{
  "name": "@pdmux/core",
  "version": "0.12.1",
  "type": "module",
  "description": "Framework-free logic for pdmux dashboards: terminal grid state, metric sparklines, usage gauges and commit-graph lane layout.",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bunx --bun tsc -p tsconfig.json",
    "test": "bunx --bun vitest run",
    "lint": "bunx --bun tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
